# This is for executing users' commands at start of a login shell. Session wide env var and to start X
# This should be linked to $ZDOTDIR/.zprofile

# The default /etc/zsh/zprofile contains a single line to source /etc/profile
# It should not be removed to ensure POSIX shell compatibility. That script sets $PATH and runs application specific
# command from /etc/profile.d/*. So don't remove it.
